# Usage and function
A basic program to take input from a user an output the average, sum, minimum, maximum of the input. The user inputs a non-negative input and finishes the input by typing a negative number, i.e input < 0.
